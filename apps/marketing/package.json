{
  "name": "@t3tools/marketing",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "npm run build:sidebar-demo && astro build",
    "build:sidebar-demo": "npm run --prefix ../web build:sidebar-demo",
    "preview": "astro preview",
    "typecheck": "astro check"
  },
  "dependencies": {
    "astro": "^7.0.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.7",
    "@vercel/config": "^0.3.0",
    "typescript": "catalog:"
  }
}
